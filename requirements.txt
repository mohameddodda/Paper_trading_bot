# Requirements for Paper Trading Bot
# Install in a virtual environment: python -m venv venv && venv\Scripts\activate (Windows) or source venv/bin/activate (Mac/Linux)
# Then run: pip install -r requirements.txt
# This is for educational paper trading simulations only. Do not use for real financial transactions.

# Core dependencies for paper trading
yfinance==0.2.28
requests==2.31.0
pandas==2.2.0
numpy==1.26.4  # Updated for compatibility
matplotlib==3.8.0
plotly==5.15.0
pyyaml==6.0  # Added for YAML config support

# Configuration and environment
python-dotenv==1.0.0

# Testing
pytest==7.4.3

# Development and code quality
black==23.9.1
isort==5.12.0
flake8==6.1.0
mypy==1.3.0
pre-commit==3.4.0

# Optional for experimentation (Jupyter notebooks)
jupyterlab==4.0.0
notebook==6.5.0
ipykernel==6.25.0

# Optional Advanced Features (for bot.py AI, UI, Cloud, etc. – Install separately if needed)
tensorflow==2.13.0  # For LSTM AI
stable-baselines3==2.0.0  # For RL AI
streamlit==1.28.0  # For UI Dashboard
websocket-client==1.6.0  # For real-time streaming
docker==6.1.0  # For containerization
boto3==1.28.0  # For AWS cloud deployment
hypothesis==6.82.0  # For property-based testing
quantstats==0.0.62  # For advanced reporting